{
  "name": "introduction",
  "version": "1.0.0",
  "description": "* npx tailwindcss -o tailwind.css (create tailwind.css file)\r * npx tailwindcss init (create tailwind.config.js)",
  "main": "tailwind.config.js",
  "scripts": {
    "build": "npx tailwindcss -i input.css -o src/output.css --minify",
    "watch": "npx tailwindcss -i input.css -o src/output.css -w --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.1.6"
  }
}
